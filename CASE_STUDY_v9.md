# 케이스 스터디 — 죽도시장 프로토타입 v9

**백엔드 없이 상인이 직접 가격을 올리는 구조를 만들고, 실사용 관점에서 디버깅한 기록**

| | |
|---|---|
| 기간 | 2026년 8월 |
| 역할 | 기획 · 설계 · 구현 · 디버깅 전담 |
| 산출물 | 단일 HTML 185KB · 2,979줄 |
| 기술 | 순수 HTML/CSS/JS — 프레임워크·번들러·백엔드 없음 |
| 데모 | https://jukdo-market-v9.vercel.app |

---

## 1. 문제

포항 죽도시장에서 같은 오징어가 가게마다 15,000원부터 42,000원까지 갈립니다.
문제는 가격 차이 자체가 아니라 **손님이 그 차이를 확인할 방법이 없다는 것**입니다.

기존 가격 비교 서비스는 이 문제를 풀지 못합니다. 전통시장 가격은 **당일·개별 상인 단위로**
바뀌는데, 그 데이터를 넣어줄 주체가 없기 때문입니다.

> **핵심 질문**
> 상인이 직접 가격을 넣게 하려면, 진입 장벽을 어디까지 낮춰야 하는가?

---

## 2. 설계 결정: 진입 장벽을 어디까지 낮출 것인가

상인은 앱을 설치하지 않고 회원가입도 하지 않는다고 전제했습니다. 그렇다고 아무나 가격을
올리게 두면 데이터를 믿을 수 없습니다. 그 사이 지점을 찾는 것이 설계의 핵심이었습니다.

| 방안 | 진입 장벽 | 신뢰도 | 판단 |
|---|---|---|---|
| 누구나 등록 | 없음 | 매우 낮음 | 데이터를 믿을 수 없음 |
| 회원가입 + 사업자 인증 | 매우 높음 | 높음 | **아무도 안 씀** |
| **오픈채팅방 PIN** | 낮음 | 중간 | **채택** |

시장 상인들이 이미 쓰고 있는 **오픈채팅방에 매일 4자리 PIN을 올리는** 방식을 택했습니다.
"그 시장 사람인지"만 거르는 최소 장치이고, 상인 입장에서는 숫자 4개를 입력하는 게 전부입니다.

손님 쪽은 반대 방향의 장치를 뒀습니다. **영수증 인증을 거친 카드에서만 후기를 쓸 수 있게**
해서, 실제로 사본 사람의 목소리만 남도록 했습니다. (v9에서는 백엔드가 없어 버튼 클릭
시뮬레이션이며, 후속 버전에서 브라우저 내 OCR로 대체됩니다.)

### 저장 계층

백엔드가 없으니 브라우저 `localStorage`를 가게별 데이터베이스처럼 썼습니다.

```
[상인 상품 화면] 저장
      │
      ├─→ localStorage "vp_products_<가게명>"   ← 영구 저장
      └─→ priceData (메모리)                    ← 손님 화면이 읽는 데이터
```

**이 이중 구조가 이후 가장 큰 버그의 원인이 됩니다.**

---

## 3. 디버깅 기록

프로토타입을 실제로 써보면서 발견한 문제들입니다. 표면 증상과 실제 원인이 달랐던 경우가
대부분이었습니다.

### 3-1. "전복이 안 돼" — 증상은 하나, 원인은 전면적

**신고된 증상**
상인 화면에서 전복 가격을 세부 가격(6개·170g·50,000원)으로 등록했는데 손님 화면에 반영되지 않음.

**처음 의심한 것**
전복 특유의 데이터 문제. 실제로 전복은 이전 버전에서 품목 종류를 못 고치는 버그가 있던
품목이라 그쪽을 먼저 봤습니다.

**실제 원인**
전복과 무관했습니다. `localStorage`에는 정상 저장되고 있었지만, **페이지를 열 때 그 데이터를
`priceData`로 다시 읽어들이는 코드가 아예 없었습니다.**

저장 직후에는 메모리의 `priceData`도 함께 갱신되니 잘 동작하는 것처럼 보입니다. 그런데
새로고침하면 `priceData`가 하드코딩된 예시 데이터로 초기화되고, `localStorage`의 저장분은
그 가게의 상인 화면을 다시 열었을 때만 그 화면에 채워질 뿐이었습니다.

> **쓰기만 있고 읽기가 없으면, 저장은 없는 것과 같습니다.**

**수정**
페이지 로드 시 `vp_products_*` 키를 전부 훑어 `priceData`에 다시 반영하도록 했습니다.
이로써 **모든 가게·모든 품목**의 등록·수정·삭제가 새로고침 후에도 유지됩니다.

**놓칠 뻔한 지점**
처음 재현을 시도할 때 파일을 `data:` URL로 열었는데, 브라우저가 그 환경에서 `localStorage`를
차단해 `SecurityError`가 났습니다. 진짜 원인이 이 에러에 가려질 뻔했습니다. 로컬 HTTP 서버로
바꿔 다시 재현한 뒤에야 실제 동작을 확인할 수 있었습니다.
**테스트 환경 자체가 잘못된 신호를 만들어낼 수 있다**는 걸 확인한 사례입니다.

---

### 3-2. "이 정보가 안 보여" — 이미 구현되어 있었던 경우

**신고된 증상**
세부 가격(총 개수·한 개당 무게)이 손님 상품 화면에 표시되지 않음.

**확인 결과**
이미 구현되어 있었습니다. 실제로 브라우저에서 전 과정을 재현해보니 메타 칩 형태로
`총 6개 · 한 개당 170g`이 정상 표시되고 있었습니다.

**진짜 요구사항**
표시 **여부**가 아니라 표시 **위치와 형식**이었습니다. 원하는 것은 가게 이름 옆에
`전복 6개 170g 50,000원` 한 줄로 이어 붙이는 형태였습니다.

> 없는 기능을 만들기 전에, 있는 기능이 어떻게 보이는지부터 확인해야 합니다.
> 재현 없이 바로 구현했다면 중복 표시가 생겼을 겁니다.

**수정**
카드 제목 줄에 세부 정보를 녹여 넣고, 중복이 된 기존 메타 칩은 제거했습니다.
세부 가격이 아닌 일반 상품은 기존 형식을 그대로 유지하도록 분기했습니다.

---

### 3-3. 과하게 지우는 초기화

**증상**
가격 종류를 "무게에 따른 가격" ↔ "개수에 따른 가격"으로 바꾸면 입력칸 세 개가 전부 비워짐.

**원인**
종류가 바뀔 때 입력 영역 전체를 다시 그리고 있었습니다. 하지만 실제로 의미가 바뀌는 건
**첫 칸(총 무게 ↔ 총 개수)뿐**이고, "한 개당 무게"와 "가격"은 그대로 유효합니다.

**수정**
다시 그리기 전에 뒤 두 칸의 값을 읽어뒀다가 복원하도록 했습니다. 첫 칸만 비워집니다.

> 상태를 초기화할 때는 **무엇이 실제로 무효해졌는지**를 따져야 합니다.
> "관련 있어 보이니 다 지운다"는 사용자에게 재입력 비용으로 돌아옵니다.

---

### 3-4. 글자가 겹치는 모달 — 부분 수정이 남긴 자국

**증상**
"가격 등록" PIN 입력창에서 제목("상인 인증")과 설명문이 6px가량 겹쳐 보임.

**원인**
`.modal-title + .modal-desc { margin-top: -6px; }`

흥미로운 건 이 규칙에 **이미 "버그 수정" 주석이 달려 있었다**는 점입니다. 원래는 모든
`.modal-desc`에 걸려 있어 체크박스·버튼 뒤 설명문까지 끌어올리던 것을, 제목 바로 다음
경우로 범위를 좁혀둔 상태였습니다.

범위는 좁혔지만 **음수 값 자체는 그대로 남아 있었습니다.** 이 모달들은 `#pinStep` 같은
단계 래퍼 구조라 부모의 `flex gap: 12px`이 제목/설명문 사이에는 적용되지 않습니다.
간격이 0인 상태에서 -6px가 걸리니 실제로 글자가 포개졌습니다.

**수정**
`margin-top: 4px`. 겹침이 사라지고 적당한 간격이 생깁니다.

> 부분 수정은 증상을 옮길 뿐 없애지 못할 때가 있습니다.
> **왜 그 값이 거기 있었는지**까지 확인해야 합니다.

---

### 3-5. 만들지 않은 기능

**요청**
"상품 삭제하면 해당 상품만 완전히 삭제되게 할 수 있어?"

**확인 결과**
이미 그렇게 동작하고 있었습니다. 두 가지 경우로 나눠 실제로 검증했습니다.

1. 이전에 저장한 적 있는 가게에서 삭제 → 새로고침 후에도 사라짐 ✅
2. 한 번도 저장한 적 없는 가게(예시 데이터만 있던 상태)에서 삭제 → 마찬가지 ✅
   (다른 가게의 같은 품목은 정상 유지)

`vpSyncToPriceData()`가 해당 가게의 기존 등록 내역을 **전부 지우고 저장분으로 통째 교체**하는
방식이라 삭제가 자연스럽게 전파되고 있었습니다.

**대신 전달한 것**
전제 조건 하나를 명확히 했습니다 — 체크 후 "상품 삭제 → 확인"만으로는 화면에서만 지워지고,
**"저장" 버튼까지 눌러야** 실제로 반영됩니다. 신고된 증상은 이 단계 누락일 가능성이 높았습니다.

> 요청받은 기능이 이미 있다면, 만드는 대신 **왜 없다고 느꼈는지**를 찾는 게 맞습니다.

---

## 4. 반복해서 확인한 패턴

- **표면 증상은 범위를 좁혀 말한다.** "전복이 안 돼"는 실제로 전 품목·전 가게 문제였습니다.
  하나가 안 되면 그것만 보지 말고 **같은 경로를 타는 다른 것들도** 확인해야 합니다.
- **재현이 구현보다 먼저다.** 5건 중 2건은 재현해보니 이미 동작하고 있었거나, 요구사항이
  신고 내용과 달랐습니다.
- **테스트 환경이 거짓말을 한다.** `data:` URL의 `localStorage` 차단처럼, 환경 제약이
  실제 버그처럼 보일 수 있습니다.
- **저장과 복원은 한 쌍이다.** 둘 중 하나만 있으면 기능이 절반만 존재합니다.

---

## 5. 남은 한계와 v35로 이어진 것

v9의 가장 큰 한계는 **데이터가 기기별로 분리된다**는 점입니다. `localStorage` 기반이라
A 상인이 등록한 가격을 B 손님은 볼 수 없습니다. 발표·시연으로는 충분하지만, "여러 사람이
같은 시세를 본다"는 서비스의 존재 이유가 성립하지 않습니다.

이 한계가 후속 작업의 출발점이 됐습니다. [v35](CASE_STUDY.md)에서는 HTML 문서가 자기 자신을
품고 있다가 상태가 바뀔 때마다 새 문서를 만들어 게시하는 **자기복제형 공유 구조**로,
서버 없이 실시간 공유를 구현했습니다.

**그 밖의 한계**
- 시드 데이터는 실제 시세가 아닌 데모용 가상 값
- 환율 고정값 (실시간 API 미연동)
- 상인 PIN 고정값 — 실제로는 서버 검증 필요
- 영수증 인증은 버튼 클릭 시뮬레이션 (v19부터 브라우저 내 OCR로 대체)

---

## 6. 배포본 검증

수정 사항이 실제 배포본에서 동작하는지 브라우저로 직접 확인했습니다.

| 확인 항목 | 결과 |
|---|---|
| 새로고침 후 상인 저장분 복원 | ✅ `localStorage` → `priceData` 반영 |
| 손님 상품 화면 세부 가격 | ✅ `바다마트 · 전복 6개 170g 50,000원` |
| 매장 상세 세부 가격 | ✅ `전복 6개 170g 50,000원` |
| 가격 종류 변경 시 첫 칸만 초기화 | ✅ 한 개당 무게·가격 유지 |
| PIN 모달 제목/설명문 간격 | ✅ 4px (겹침 없음) |
| 콘솔 에러 | 없음 |
