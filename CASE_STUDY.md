# 케이스 스터디 — 죽도시장 가격비교 프로토타입

**서버 없이 실시간 가격 공유를 구현한 단일 HTML 프로토타입**

| | |
|---|---|
| 기간 | 2026년 8월 (v4 → v35, 35회 반복) |
| 역할 | 기획 · 설계 · 구현 전담 |
| 산출물 | 단일 HTML 977KB · 실제 소스 약 8,200줄 |
| 기술 | 순수 HTML/CSS/JS — 프레임워크·번들러·백엔드 없음 |
| 데모 | https://jukdo-market.vercel.app |
| 문서 | [README](README.md) · [v9 프로젝트](https://github.com/kimminseong8917-cloudwork/jukdo-market-v9#readme) |

---

## 1. 문제

포항 죽도시장에서 같은 오징어가 가게마다 15,000원부터 42,000원까지 갈립니다.
문제는 가격 차이 자체가 아니라 **손님이 그 차이를 확인할 방법이 없다는 것**입니다.

시장의 가격 정보는 세 갈래로 끊겨 있었습니다.

- **손님** — 발품을 팔아 하나씩 물어보는 것 외에 비교 수단이 없음
- **상인** — 내 가격이 시세 대비 싼지 비싼지 알 수 없어 감으로 정함
- **외국인 관광객** — 언어 장벽 때문에 물어보는 것조차 어려움

기존 가격 비교 앱들은 이 문제를 풀지 못합니다. 전통시장 가격은 **당일·개별 상인 단위로**
바뀌는데, 그 데이터를 넣어줄 주체가 없기 때문입니다.

> **핵심 질문**
> 상인이 직접 가격을 넣게 하려면, 진입 장벽을 어디까지 낮춰야 하는가?

---

## 2. 제약 조건이 설계를 결정했다

프로토타입 단계에서 세 가지 제약이 있었습니다.

1. **백엔드를 만들 수 없다** — 서버·DB·배포 파이프라인을 세울 여건이 아니었음
2. **상인이 앱을 설치하지 않는다** — 설치·회원가입을 요구하면 아무도 안 씀
3. **그럼에도 여러 사람이 같은 데이터를 봐야 한다** — 이게 없으면 서비스가 성립 안 함

1번과 3번은 정면으로 충돌합니다. 서버 없이 데이터를 공유해야 한다는 뜻이니까요.

### 검토한 선택지

| 방안 | 장점 | 탈락 이유 |
|---|---|---|
| `localStorage`만 사용 | 가장 간단 | 기기별로 데이터가 갈라져 **공유가 아예 안 됨** |
| Firebase 등 BaaS | 실시간 동기화 표준 | 가입·키 관리 필요, "설치 없이" 원칙과 충돌 |
| **자기복제형 문서** | 서버 0, 링크 하나로 공유 | 구현 난도 높음 — **채택** |

---

## 3. 해결책: 문서가 스스로를 다시 써내는 구조

HTML 문서 안에 **자기 자신의 원본 복사본**을 넣어두고, 상태가 바뀔 때마다 그 틀에 새
데이터를 주입해 **새 문서를 만들어 게시**하는 방식을 택했습니다.

```
┌─ HTML 문서 ────────────────────────────────────┐
│  <script id="app-state">  ← 지금까지의 공유 상태(JSON)     │
│  <script id="doc-shell">  ← 자기 자신의 원본 틀            │
│  <script> 앱 로직 </script>                                │
└───────────────────────────────────────────────┘

부팅   : #app-state 읽어 priceData 복원
저장   : #doc-shell 틀 + 새 상태 → 완성된 새 HTML 생성 → publish()
결과   : 모든 접속자가 갱신된 문서를 봄
```

이 한 가지 결정으로 세 제약이 동시에 풀렸습니다. 서버가 없고, 설치가 없고, 링크 하나로
공유됩니다. 대신 **자기 자신을 다루는 코드 특유의 함정**을 전부 떠안게 됐습니다.

### 함정 1 — 스크립트가 중간에 끊긴다

소스 안에 `</script>` 문자열이 **주석이든 문자열이든** 그대로 있으면, 브라우저는 그 지점에서
스크립트가 끝난 것으로 판단합니다. 자기 자신을 문자열로 품는 구조에서는 반드시 발생합니다.

```js
// 이렇게 조각내어 실행 시점에만 합쳐지도록 함
return s.split('<\\/script').join('<' + '/' + 'script');
```

### 함정 2 — 치환 코드가 자기 자신을 치환한다

자리표시자를 찾아 바꾸는 코드가 `#doc-shell` 안에 복사될 때, 그 치환 로직이 **자기 코드의
그 줄까지** 찾아서 망가뜨립니다. 상수를 문자열 조각으로 나눠 회피했습니다.

```js
const STATE_TAG = '<script id="app-state" type="application/json">'
                + '__APP_STATE_JSON__' + '</' + 'script>';
```

### 함정 3 — `String.replace`의 `$` 해석

치환 문자열의 `$&`·`$1`은 특수 패턴으로 해석됩니다. 이 문서엔 `${...}` 템플릿 리터럴이
수백 개라 실제로 터집니다. 전부 `split().join()`으로 바꿨습니다.

### 함정 4 — 동시 저장 경쟁 상태

30인 동시접속을 가정하고 코드를 다시 읽다가 발견했습니다. 같은 항목에 게시가 겹치면
**나중에 응답이 오는 쪽이 먼저 온 쪽을 덮어씁니다.** 같은 키의 게시가 진행 중이면 새 시도를
무시하도록 잠금을 걸었습니다.

### 함정 5 — 저장하면 화면이 튕긴다

게시에 성공하면 문서가 새 버전으로 새로고침됩니다. 사용자 입장에선 후기를 쓰자마자
**첫 화면으로 튕겨나가는** 셈이었습니다. 새로고침 직전에 보던 화면을 `sessionStorage`에
적어두고 부팅 시 복원하도록 했습니다.

---

## 4. 신뢰 문제: 누가 가격을 보증하는가

가격을 아무나 올릴 수 있으면 데이터를 믿을 수 없습니다. 두 층으로 방어했습니다.

**상인 측 — PIN 인증**
오픈채팅방에 매일 올라오는 4자리 PIN을 입력해야 가격을 등록할 수 있습니다.
회원가입 없이 "그 시장 사람인지"만 거르는 최소 장치입니다.

**손님 측 — 영수증 OCR**
초기에는 버튼 한 번으로 인증을 흉내만 냈는데, 이래서는 후기의 신뢰도가 없었습니다.
브라우저 안에서 도는 OCR(Tesseract.js)로 **영수증 사진의 가게명·금액을 실제로 대조**하도록
바꿨습니다. 서버 전송이 없어 개인정보 문제도 피했습니다.

OCR은 지저분한 결과를 냅니다. 그래서 관용도를 넉넉하게 잡았습니다.

- 가게명 — 공백 제거 후 포함 검사 → 실패하면 편집거리(Levenshtein) 슬라이딩 윈도 비교
- 금액 — 완전 일치 대신 **5% 오차 허용** (자릿수 하나 오인식으로 매번 실패하는 걸 방지)
- 라이브러리 로딩 실패·인식 불확실 시 → **사진 첨부 방식으로 자동 강등** (기능이 죽지 않음)

---

## 5. 관점을 넷으로 나눠 기능을 뽑다

v35에서는 사용자를 네 관점으로 나눠 각각의 미충족 지점을 훑고 기능 12종을 도출했습니다.

### 손님
| 기능 | 해결한 불편 |
|---|---|
| 목표가 알리미 | 원하는 가격이 될 때까지 매번 다시 확인해야 했음 |
| 매장 비교함 | 카드를 오가며 가격을 **암산으로** 비교해야 했음 |
| 장보기 최적 플랜 | 여러 품목을 살 때 어디서 뭘 살지 조합을 못 짰음 |

### 외국인 관광객
| 기능 | 해결한 불편 |
|---|---|
| 4개 언어 + 통화 환산 | 원화 금액이 비싼지 싼지 감이 안 옴 |
| 문구집 음성 재생 | 읽을 수 있어도 **발음할 수 없어** 말을 못 걺 |

### 상인
| 기능 | 해결한 불편 |
|---|---|
| 내 가게 성과 요약 | 내 가격이 시세 대비 어디쯤인지 눈으로 세어봐야 했음 |
| 실시간 입력 힌트 | 가격을 정한 뒤에야 비싼 걸 알게 됨 |
| 후기 요약 | 손님 반응을 확인할 경로가 없었음 |

### 시장 전체
실시간 소식 · 오늘의 특가 · 신뢰 랭킹을 한 화면에 묶어, 개별 품목을 넘어 **시장 전체의
활기**를 보여주도록 했습니다.

---

## 6. 반복 과정에서 배운 것

35번의 버전을 거치며 되풀이해 확인한 패턴들입니다.

**"겹쳐 보인다"는 대개 간격 문제가 아니라 규칙의 적용 범위 문제**
모달 제목과 설명문이 겹치는 버그가 있었습니다. 원인은 `margin-top: -6px`였는데, 이 규칙은
원래 다른 맥락을 위해 만들어진 것이었습니다. 부모의 `flex gap`이 적용되지 않는 구조에서
음수 마진만 남으면서 실제로 글자가 포개졌습니다. **간격을 늘리는 대신 규칙의 범위를 좁히는
것**이 맞는 수정이었습니다.

**저장했는데 사라지는 데이터는, 대개 "저장"과 "복원"이 짝이 안 맞는 것**
상인이 등록한 가격이 새로고침하면 사라지는 버그가 있었습니다. 저장은 `localStorage`에
제대로 되고 있었지만, **페이지를 열 때 그 데이터를 다시 읽어들이는 코드가 없었습니다.**
쓰기만 있고 읽기가 없으면 저장은 없는 것과 같습니다.

**한 번에 다 지우는 초기화는 대체로 과하다**
가격 종류(무게 기준 ↔ 개수 기준)를 바꾸면 입력칸 세 개가 전부 비워졌습니다. 실제로 의미가
바뀌는 건 첫 칸뿐인데, 나머지 두 개까지 다시 입력하게 만들고 있었습니다.

**동시접속을 가정하고 코드를 다시 읽으면 새 버그가 나온다**
혼자 쓸 때는 절대 안 보이던 경쟁 상태가, "30명이 동시에 누르면?"이라는 질문 하나로
드러났습니다. 기능 추가만큼이나 **가정을 바꿔 다시 읽는 것**이 효과적이었습니다.

---

## 7. 트레이드오프: 어디에 배포하느냐가 기능을 바꾼다

이 프로젝트에서 가장 흥미로운 제약입니다. **배포 위치에 따라 살아있는 기능이 달라집니다.**

| | Claude Artifact | 정적 호스팅(Vercel 등) |
|---|---|---|
| 실시간 가격 공유 | ✅ **핵심 기능 동작** | ❌ 내 브라우저에만 저장 |
| 영수증 OCR | ⚠️ CSP로 CDN 차단 → 사진 첨부로 강등 | ✅ 동작 |
| 지도 · 품목 사진 | ⚠️ 차단될 수 있음 | ✅ 동작 |

자기복제 구조는 `window.claude.use('artifact')`에 의존합니다. 그게 없는 환경에서는
`publishSharedState()`가 `'local-only'`를 반환하고 "이 화면에서만 보여요" 배너를 띄운 뒤
로컬에만 반영합니다. **앱이 깨지지는 않지만, 서비스의 존재 이유인 공유 기능이 사라집니다.**

실제로 Vercel에 배포해 확인한 결과, 예측한 그대로였습니다 — `claude.use('artifact')`는 `null`,
`publishSharedState()`는 `'local-only'`, 대신 OCR 라이브러리와 품목 사진은 정상 로드됐고
콘솔 에러는 없었습니다. **설계 단계에서 가정한 강등 경로가 실제로 안전하게 동작한 것**입니다.

반대로 Artifact 환경은 엄격한 CSP 때문에 외부 CDN·이미지·지도가 막힙니다.
다행히 OCR은 실패 시 사진 첨부 방식으로 강등되도록 이미 설계해둬서 기능이 죽지는 않습니다.

> **정리** — 실시간 공유를 보여주려면 Artifact, OCR·지도까지 온전히 보여주려면 정적 호스팅.
> 하나로 둘 다 만족시키려면 백엔드가 필요하며, 그게 이 프로토타입의 다음 단계입니다.

---

## 8. 결과와 다음 단계

**만든 것**
서버 없이 여러 사람이 같은 가격 데이터를 보는 프로토타입. 설치·회원가입 없이 링크 하나로
접근하고, 상인은 PIN만으로 가격을 올리며, 손님은 영수증으로 인증된 후기를 남깁니다.

**한계**
- 시드 데이터는 실제 시세가 아닌 데모용 가상 값
- 환율 고정값 (실시간 API 미연동)
- 동선은 구역 번호 순 정렬이지 최단 경로가 아님
- 상인 PIN 고정값 — 실제로는 서버 검증 필요

**다음 단계**
1. 실제 상인 대상 사용성 테스트 — PIN 인증의 진입 장벽이 정말 충분히 낮은지 검증
2. 경량 백엔드 도입 — 배포 환경에 따라 기능이 갈리는 문제 해소
3. 실제 시세 데이터 확보 — 데모 값을 실측치로 교체
